import React from "react";
import PropTypes from "prop-types";

import "./Feed.scss";

const Feed = props => {
  return (
    <div className="feed-container">
      <h1>feed</h1>
    </div>
  );
};

Feed.propTypes = {};

export default Feed;
